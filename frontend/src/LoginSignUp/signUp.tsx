import React, { useState, useRef, useEffect} from 'react';

import observer from 'mobx-react';

import { IInputs } from '../FormInput/Input';
import { Input } from '../FormInput/Input';


const SignUpComponent = ( ) => {
    return (
        <div>

        </div>
    )
}